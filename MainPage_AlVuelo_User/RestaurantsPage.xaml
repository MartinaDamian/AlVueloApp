<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             x:Class="AlVueloMobile.Views.RestaurantsPage"
             Title="Restaurantes">
    <VerticalStackLayout Padding="16" Spacing="12">
        <Label Text="Restaurantes disponibles" FontSize="22"/>

        <CollectionView ItemsSource="{Binding Restaurantes}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="12" Margin="0,6" CornerRadius="14">
                        <VerticalStackLayout Spacing="6">
                            <Label Text="{Binding nombre}" FontSize="18"/>
                            <Label Text="{Binding ubicacion}" FontSize="12"/>
                            <Label Text="{Binding horario}" FontSize="12"/>
                            <Button Text="Ver menú"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenRestaurantCommand}"
                                    CommandParameter="{Binding}" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>
